--- DIAGNOSE WHY VM CREATION IS BLOCKED ---

# Check Azure policy assignments (may show the "best available regions" rule)
az policy assignment list --query "[].{name:name, policy:policyDefinitionId, scope:scope}" -o table

# Check if Microsoft.Compute resource provider is registered
az provider show -n Microsoft.Compute --query "registrationState" -o tsv

# Register if needed:
az provider register -n Microsoft.Compute

# Try a single VM creation with full error output
az group create -n testrg -l westus -o none
az vm create -g testrg -n testvm --image Ubuntu2204 --size Standard_B1s --admin-username azureuser --generate-ssh-keys
az group delete -n testrg --yes



--- AFTER DEPLOY ---

# App URL (printed at end of script)
http://<IP>:3000
Admin login: http://<IP>:3000/login
User: admin / weguysfromcodingninjas



--- STOP VM (save $) ---
az vm deallocate -g ReverseCodeRG -n ReverseCodeVM

--- START VM ---
az vm start -g ReverseCodeRG -n ReverseCodeVM

--- DELETE EVERYTHING ---
az group delete -n ReverseCodeRG --yes
