--- STEP 1: FIND AN ALLOWED REGION ---
# Azure for Students has a "best available regions" policy.
# Run each command one at a time until one returns "Succeeded":

az group create -n testrg -l westeurope --query properties.provisioningState
az group delete -n testrg --yes

# If westeurope fails, try (one at a time, replace westeurope):
#   northeurope
#   westus
#   eastus
#   southeastasia
#   eastus2
#   uksouth
#   australiaeast
#   francecentral
#   centralus
#   canadaeast

# Once "Succeeded" appears, note the region name (e.g. westeurope)



--- STEP 2: DEPLOY (replace REGION with the one that worked) ---
az group delete -n ReverseCodeRG --yes
curl -sL https://raw.githubusercontent.com/sundhar-04/Reverse-Engineering-Contest/main/deploy-azure.sh | bash -s -- REGION



--- AFTER DEPLOY ---

# App URL (printed at end of script)
http://<IP>:3000
Admin login: http://<IP>:3000/login
User: admin / weguysfromcodingninjas



--- STOP VM (save $) ---
az vm deallocate -g ReverseCodeRG -n ReverseCodeVM

--- START VM ---
az vm start -g ReverseCodeRG -n ReverseCodeVM

--- DELETE EVERYTHING ---
az group delete -n ReverseCodeRG --yes
